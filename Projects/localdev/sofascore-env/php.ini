; Mounted into the tools container as conf.d/zz-local.ini.
; CLI-only container: match Debian's php-cli default (no memory cap) — Symfony's
; container compile / cache warmup for this app blows past PHP's default 128M.
memory_limit = -1
