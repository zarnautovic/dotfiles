; Mounted into the tools container as conf.d/zz-local.ini — loaded AFTER the
; project's own docker/base/php/99-sofa.ini (apc.enable_cli, date.timezone=UTC,
; memory_limit=1024M, opcache…), so anything here overrides it.
; Intentionally empty: keep parity with the project unless there's a reason.
